{
  "name": "erc20-token-sample",
  "version": "1.0.0",
  "description": "Production-ready ERC20 token with burn capability",
  "type": "module",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "coverage": "hardhat coverage",
    "lint": "solhint 'contracts/**/*.sol'",
    "clean": "hardhat clean",
    "setup-compiler": "node scripts/setup-compiler.js"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-ethers": "^4.0.15",
    "chai": "^4.5.0",
    "dotenv": "^16.0.0",
    "ethers": "^6.17.0",
    "hardhat": "^3.11.0",
    "solhint": "^5.0.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0"
  }
}
