{
  "name": "cf-orchestrator",
  "version": "1.0.0",
  "description": "creatingfire.org — Central Orchestration Engine on Cloudflare Workers",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:staging": "wrangler deploy --env staging",
    "typecheck": "tsc --noEmit",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240520.0",
    "typescript": "^5.4.5",
    "wrangler": "^3.57.0"
  }
}
